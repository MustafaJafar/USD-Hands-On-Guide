#usda 1.0

# What color will be the cube ? 
(
    subLayers = [
        @./a_red_cube.usda@
    ]
)

over "a_cube"
{
    # Local Edit.
    color3f[] primvars:displayColor = [(0, 1, 0)] (
        interpolation = "constant"
    )
}